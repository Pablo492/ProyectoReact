import React from 'react';

const Question = (props) => {
    return(
        <div>
            <p>{title}</p>
            <p>{answersAmount}</p>
            <p>{votes}</p>
            <p>{views}</p>
        </div>
    );
};

export defaut Question;